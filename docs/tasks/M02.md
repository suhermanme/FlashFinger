# M02 — Shared Vite and secure Electron shell

## Completion record

Date: 2026-09-20
Status: complete

## Deliverables

### 1. Shared TypeScript/React/Vite renderer

| File | Description |
|---|---|
| `src/app/bootstrap.tsx` | Renderer entry point (`index.html` `<script type="module" src="./...">`). Renders `App` into `#root`. |
| `src/app/App.tsx` | Shared renderer shell displaying platform capability badges. |
| `vite.config.ts` | Unchanged from M01 — React + Tailwind plugins, `base './'`, outDir `dist/renderer`. |
| `tsconfig.app.json` | Unchanged — DOM + DOM.Iterable libs, `react-jsx` jsx, path alias `@ → src`. |

### 2. Electron main/preload/protocol

| File | Description |
|---|---|
| `electron/main.ts` | Sandbox-bridged `BrowserWindow` (`nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`), single-instance lock guard, `flashfinger://` protocol registration, `dist/renderer/` load. |
| `electron/preload.ts` | `contextBridge.exposeInMainWorld` with channel whitelist (30+ `FF_IPC_CHANNELS`), mock `'unavailable'` responses. |
| `electron/protocol.ts` | `protocol.handle()` Promise API handler — path traversal rejection (`..`, absolute paths), MIME type mapping, CSP `default-src 'self'`, serves from `dist/renderer/`. |
| `electron/tsconfig.electron.json` | Fixed `moduleResolution: "bundler"` (TS7 compatible), added `"electron"` types, `module: "CommonJS"`, outDir `dist/electron`. |

### 3. Platform detection and capability bridge

| File | Description |
|---|---|
| `src/platform/factory.ts` | `getPlatformAdapter()` — detects `FF_BRIDGE_GLOBAL` on `globalThis` (safe in Node/browser/Electron), returns `PlatformAdapter` with `target`, `capabilities`, and `repository`. Cached singleton. |
| `src/platform/mock-repository.ts` | Full `Repository` interface implementation returning `RepositoryResult.err({ code: 'unavailable', retryable: true })` for all 33 methods. |

### 4. Static ready-to-configure shell

- `index.html` — updated to reference `src/app/bootstrap.tsx` entry point.
- `package.json` — added `"main": "dist/electron/main.js"` for Electron entry point.

### 5. Test wiring

| File | Description |
|---|---|
| `tests/integration/shell.test.ts` | 41 tests: browser root build (3), platform factory (2), mock repository 31 methods (31), preload whitelist (1), renderer isolation (2), nested-path build (2). |

### 6. Verification results

All checks pass:

- `npm run typecheck` — 0 errors across 3 TS configs.
- `npm run build` — produces `dist/renderer/index.html` + single JS bundle with relative `./` paths.
- `npm run electron:compile` — compiles electron/ to `dist/electron/`.
- `npm test` — 41/41 tests pass.

## Exclusions (by spec)

- No typing engine (M03)
- No real persistence/backend (M04/M05)
- No PWA caching (M16)
- No M01 changes

## Notes

- Protocol uses modern `protocol.handle()` API (Electron ≥27), not deprecated callback-based `registerFileProtocol`.
- Platform factory uses `globalThis` for bridge detection — testable in Node environment.
- Single renderer artefact (`dist/renderer/`) shared by browser and Electron.
- Mock repository satisfies the full `Repository` interface contract.
