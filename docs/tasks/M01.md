# M01 — Record contracts and test fixtures: completion record

Completed: `2026-09-20T10:15:51Z` (UTC)

M01 status: **complete**.

The task implemented only shared TypeScript contracts, pure runtime validation, deterministic fixtures, and handoff documentation. It did not implement screens or storage.

## Outputs

| Required output | Actual path | State |
|---|---|---|
| Models | `src/contracts/models.ts` | complete |
| Repository contract | `src/contracts/repository.ts` | complete |
| Platform contract | `src/contracts/platform.ts` | complete |
| Training contract | `src/contracts/training.ts` | complete |
| Runtime validation | `src/contracts/validation.ts` | complete |
| Contract fixtures | `tests/fixtures/contracts/index.ts` | complete |
| Implementation status | `docs/IMPLEMENTATION_STATUS.md` | complete |
| Decisions | `docs/DECISIONS.md` | complete |

`src/contracts/versions.ts` is the documented extra output that centralizes version identities.

## Validation coverage

Public record entry points cover `PracticeConfig`, `ProfileSettings`, `Profile`, `InstallationSettings`, `SessionConfig`, `SessionRecord`, `MetricSample`, `SessionSeries`, `SessionDaySlice`, `MistakeBucket`, `CharacterExposure`, `LessonProgress`, `DailyAggregate`, `CustomDocument`, `DocumentChunk`, `CheckpointCounts`, `CheckpointEdit`, `ActiveCheckpoint`, `ProfileSummary`, `BackupPayloads`, and `BackupEnvelope`.

`validateOwnershipReferences` checks active-profile, session/profile, progress/session, document/chunk, aggregate/profile, and checkpoint/profile references. `validateBackupEnvelope` composes structural and ownership validation. `checkSchemaVersion`, `checkBackupFormatVersion`, `checkMetricVersion`, and `checkCurriculumVersion` return repository-compatible `version-too-new` failures for unsupported future versions.

The validators also reject unknown fields and enum members, non-plain objects, undefined required values, invalid identifiers/timestamps/dates/zones, negative or unsafe counters, NaN/Infinity, impossible attempt totals, invalid nullable-rate states, and over-cap arrays/strings.

## Fixture inventory

`tests/fixtures/contracts/index.ts` exports:

- `profileA`, `profileB`, and `profiles`
- `completedSession`
- `interruptedSession`
- `failedLessonSession`, `failedLessonProgress`, and `failedLesson`
- `emptyHistory`
- `midnightSession` and `midnightSessionDaySlices`
- `sessions` plus stable IDs for cross-record tests

The midnight fixture runs from `2026-09-20T16:59:30Z` to `17:01:30Z`, crossing from 2026-09-20 to 2026-09-21 in `Asia/Jakarta`, with source counters split across both dates.

## Verification

```text
npx tsc -p tsconfig.app.json --noEmit   -> exit 0
npx tsc -p tsconfig.test.json --noEmit  -> exit 0
```

Runtime verification used Vite's in-process SSR module loader and exited 0 with:

```text
runtime validation matrix passed (21 validators, ownership accept/reject, malformed record/date rejection, 4 version gates)
```

The matrix exercised every public record validator, nested backup validation, ownership acceptance and rejection, rejection of an unknown status/field, NaN, and an impossible calendar instant, plus future schema/backup/metric/curriculum versions.

## Deferred to M02 or later

- Permanent executable test wiring and contract test files: M02 explicitly owns test tooling.
- Missing renderer/Electron entries and TypeScript 7 Electron configuration repair: M02.
- Canonical metric recomputation: M03.
- Backup checksum generation/recomputation and staged import: M16.

The partial M02 scaffold remains red for those documented reasons; it is not an M01 acceptance failure.
