import type { HistoricalBar } from '../../domain/metrics/aggregates.js';
import type { CalendarDay } from '../../domain/metrics/calendar.js';
export function HistoryDashboard({ bars, calendar }: { bars: readonly HistoricalBar[]; calendar: readonly CalendarDay[] }) { return <section className="ff-history" aria-labelledby="history-heading"><h2 id="history-heading">History</h2><div className="ff-history-bars" aria-label="Historical performance">{bars.map((bar) => <article key={bar.day}><strong>{bar.day}</strong><span>{bar.adjustedWpm == null ? '—' : `${bar.adjustedWpm.toFixed(1)} WPM`}</span><small>{bar.sessions} session{bar.sessions === 1 ? '' : 's'}</small></article>)}</div><div className="ff-calendar" aria-label="Activity calendar">{calendar.map((day) => <span key={day.day} title={`${day.day}: ${day.activeMs} ms`} data-level={day.level} />)}</div></section>; }
